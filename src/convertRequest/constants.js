/** Supported target case names for the HTTP API and `wordsToCase`. */
const SUPPORTED_CASES = Object.freeze([
  'SNAKE',
  'KEBAB',
  'CAMEL',
  'PASCAL',
  'UPPER',
]);

module.exports = {
  SUPPORTED_CASES,
};
